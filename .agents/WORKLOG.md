# WORKLOG

## 2026-04-05

- Инициализирован отдельный каталог `project/` как самостоятельный git-репозиторий.
- Добавлены базовые служебные файлы: `README.md`, `.gitignore`, `.agents/WORKLOG.md`.
- Добавлен `.agents/REFACTOR.md` с полным планом рефакторинга пайплайна:
  от EDA до экспериментальных ноутбуков и final submission для нетяжелых моделей.
- Создана локальная рабочая структура `project/notebooks/` для рефакторинга без правок
  исходных ноутбуков в корне репозитория.
- Добавлены новые ноутбуки:
  `01_prepare_eda.ipynb`, `02_mean_baseline.ipynb`, `04_tabular_baselines.ipynb`,
  `06_compare_models.ipynb`.
- Перенесены и переименованы существующие ноутбуки:
  `03_sarima_store_level.ipynb`, `05_catboost.ipynb`,
  `archive_02_sarima_solution.ipynb`.
- В `05_catboost.ipynb` базовый набор численных признаков очищен от `sales_sum`;
  добавлено разделение между безопасными submission-признаками и optional research features.

- Репозиторий переведен на схему `data/raw -> artifacts/* -> notebooks/*` для полной воспроизводимости.
- Из `project/data` убрана зависимость от общего симлинка на корневой `data/`; вместо этого созданы только raw-ссылки.
- Все ноутбуки переведены на единые пути к `artifacts/` и очищены от старых save-path в `data/experiment_info`.
- В ноутбуках обновлены markdown-заголовки и вступительные блоки: теперь каждый ноутбук явно фиксирует, что читает, что сохраняет и какое место занимает в пайплайне.
- В `04_tabular_baselines.ipynb` добавлен recursive evaluation и шаблон полного submission для нетяжелых моделей.
- В `05_catboost.ipynb` сохранен proxy-tuning через Optuna, но финальная отчетная оценка явно отделена как recursive CV.
- `01_prepare_eda.ipynb` усилен содержательно на основе исходного `01_eda_baseline.ipynb`:
  возвращены расширенные блоки EDA по полноте данных, справочникам, внешним факторам,
  целевой переменной, нулевым продажам и похожести магазинов.
- В `01_prepare_eda.ipynb` восстановлена подготовка полной обучающей панели
  `date x store_nbr x family` с заполнением отсутствующих наблюдений нулевыми продажами.
- Текстовые markdown-блоки в `01_prepare_eda.ipynb` переписаны в более динамичном стиле:
  добавлена явная мотивация перед секциями и шаблоны промежуточных выводов после результатов.
- В местах, где вывод зависит от фактического output, оставлены заметки для дописывания
  после запуска ноутбука.
- Markdown-пояснения по ноутбукам дополнительно очищены от лишней служебной мета-лексики
  (`артефакты`, `README`, `пайплайн`, отсылки к старым данным) и приведены к более
  естественному исследовательскому стилю.

## 2026-04-12

- В `project/notebooks/01_prepare_eda.ipynb` существенно усилены аналитические выводы:
  возвращены инсайты из исходного `notebooks/01_eda_baseline.ipynb`, добавлены
  интерпретации по сезонности, событиям, `transactions`, `oil`, ассортиментной разреженности
  и ограничениям признаков для реального прогноза.
- В `project/notebooks/02_mean_baseline.ipynb` добавлен итоговый исследовательский комментарий
  по качеству mean-baseline как нижней контрольной точки.
- В `project/notebooks/04_tabular_baselines.ipynb` добавлен итоговый блок с интерпретацией
  результатов ridge относительно mean-baseline на `all_series` и `top_pairs`.
- В `project/notebooks/06_compare_models.ipynb` добавлены итоговые выводы по сравнению моделей
  и fallback-загрузка метрик из `data/experiment_info`, чтобы сводка подтягивала `sarima` и `catboost`
  даже без переноса этих файлов в `project/artifacts/metrics`.
- Проверено, что обновлённые ноутбуки `01_prepare_eda.ipynb`, `02_mean_baseline.ipynb`,
  `04_tabular_baselines.ipynb` и `06_compare_models.ipynb` остаются валидными JSON-файлами.
- Что проверить дальше: вручную открыть ноутбуки в Jupyter/VS Code и убедиться,
  что новые markdown-блоки стоят в удачных местах относительно output-таблиц и графиков.

- В `project/notebooks/01_prepare_eda.ipynb` добавлены новые кодовые EDA-блоки:
  лаговый анализ связи `sales` с `dcoilwtico` и `is_payday` по товарным семействам,
  сводка по лучшим лагам, heatmap профилей лаговых корреляций, а также расширенный
  анализ временной структуры рядов с ACF, выделением тренда, оценкой выбросов,
  праздничного uplift и эффекта окна после землетрясения 2016-04-16.
- После новых кодовых ячеек в `01_prepare_eda.ipynb` добавлены markdown-заготовки,
  которые явно фиксируют, какие именно выводы нужно будет дописать после повторного прогона.
- Что проверить дальше: прогнать новый EDA-блок в `01_prepare_eda.ipynb`, убедиться,
  что таблицы и графики читаемы по размеру, и затем на основе output дописать итоговые выводы.
- В `project/notebooks/01_prepare_eda.ipynb` заполнены итоговые markdown-выводы
  после новых EDA-блоков по лаговым внешним факторам и временной динамике.
  Выводы переписаны по фактическому output после прогона ноутбука: добавлены
  интерпретации по лагам нефти, payday-эффекту, ограничению `transactions`,
  силе `onpromotion`, недельной и месячной сезонности, прерывистому спросу,
  выбросам, праздничным и постземлетрясным всплескам.
- Что проверить дальше: визуально открыть `01_prepare_eda.ipynb` и убедиться,
  что объём новых markdown-блоков хорошо читается в интерфейсе ноутбука и не
  требует дополнительного разбиения на подзаголовки.
